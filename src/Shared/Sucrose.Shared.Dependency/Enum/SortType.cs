namespace Sucrose.Shared.Dependency.Enum
{
    internal enum SortKindType
    {
        Ascending,
        Descending
    }

    internal enum SortModeType
    {
        Name,
        Creation,
        Modification
    }
}